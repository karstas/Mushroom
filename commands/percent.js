module.exports = {
  name: 'percent',
  execute(mushroom, message, args){
    message.channel.send(`Work in Progress <:dorime:685494997088469021>`)
    /*args = args.join(' ').split(',');
    const valor1 = 101
    var escolha1 = args[Math.floor(Math.random()*args.length)];
    var args1 = Math.floor(Math.random()*valor1);*/
  }
}
